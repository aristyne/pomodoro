Pomodoro Desktop App (Electron)

Overview
- A calm, minimal Pomodoro timer designed for students.
- Frameless window with custom chrome (close/settings).
- Themeable UI with at least three themes.
- Persisted settings and session progress using local storage in the renderer and config.json in the user data folder.

How to Run
- Install dependencies: npm install
- Start: npm run start
